<div id="<%= dom_id post %>">
  <p>
    <strong>Content:</strong>
    <%= post.content %>
  </p>

  <p>
    <strong>Tag:</strong>
    <% post.tags.each do |tag| %>
      <%= tag.name %>
    <% end %>
  </p>
</div>
